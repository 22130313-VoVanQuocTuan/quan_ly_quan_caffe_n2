{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "node",
    "allowJs": true,  // Cho phép sử dụng JavaScript
    "checkJs": false, // Không kiểm tra lỗi JS, nếu không muốn kiểm tra
    "noEmit": true     // Không xuất file khi build
  },
  "include": [
    "src/**/*",        // Bao gồm tất cả các file trong thư mục src
    "vite.config.js"   // Bao gồm file cấu hình của Vite
  ],
  "exclude": [
    "node_modules",    // Loại trừ thư mục node_modules
    "dist"             // Loại trừ thư mục build
  ]
}
