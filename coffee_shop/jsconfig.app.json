{
  "compilerOptions": {
    "target": "ES2020",                   // Đặt phiên bản JavaScript mục tiêu
    "module": "ESNext",                   // Đặt module dạng ESNext
    "moduleResolution": "node",           // Cách giải quyết module theo kiểu Node.js
    "allowJs": true,                      // Cho phép sử dụng file JS trong dự án
    "checkJs": false,                     // Không kiểm tra lỗi JS (nếu muốn kiểm tra, để true)
    "noEmit": true,                       // Không xuất các file khi build
    "jsx": "react-jsx"                    // Nếu dùng React, thiết lập JSX cho React 17 trở lên
  },
  "include": [
    "src/**/*",                           // Bao gồm tất cả các file trong thư mục src
    "vite.config.js"                      // Bao gồm file cấu hình Vite nếu cần
  ],
  "exclude": [
    "node_modules",                       // Loại trừ thư mục node_modules
    "dist"                                // Loại trừ thư mục build
  ]
}
